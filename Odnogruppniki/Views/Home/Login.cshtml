
@{
    ViewBag.Title = "Login";
}

<br /><br /><br />

<script type="text/javascript">
    function login() {
        var log = $("#login").val();
        var pass = $("#pass").val();
        var data = {
            'login': log,
            'password': pass
        };
        $.ajax({
            type: 'POST',
            data: data,
            url: '@Url.Action("Login", "Home")',
            dataType: 'json',
            success: function (data) {
                if (data.Success) {
                    window.location = '@Url.Action("Index","Home")';
                } else {
                    alert(data.Error);
                }
            }
        });
    }
</script>

<div class="table" style="width:20%; margin-left:40%">
    <div class="row">
        <h4>Логин</h4>
        <input type="text" name="login" id="login" />
    </div>
    <div class="row">
        <h4>Пароль</h4>
        <input type="password" name="pass" id="pass" />
    </div>
    <div class="row" style="margin-top:15px">
        <input class="form-control" type="button" id="editButton" onclick="login()" value="Вход" style="height:inherit; width:47%; border-radius:8px; background-color:cornflowerblue; color:ivory" />
    </div>
</div>

